services:
  app:
    build: .
    ports:
      - "8081:8081"
    environment:
      - EXPO_PUBLIC_API_URL=http://localhost:8000
    # Removed volume mounting to ensure we serve the built assets from the container's image
    # and removed command override to use the Dockerfile's CMD
